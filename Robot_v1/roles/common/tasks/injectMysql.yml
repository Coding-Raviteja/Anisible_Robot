- name: Installing mysql for schema
  ansible.builtin.package:
    name: mysql
    state: present

- name: Injecting schema
  loop: "{{ schema }}"
  ansible.builtin.shell: mysql -h mysql-{{ env }}.robobuy.site -uroot -p{{ secrets['mysql_pass'] }} < /app/db/{{ item }}